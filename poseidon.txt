version 2.0.0;
circuit;
@type field 21888242871839275222246405745257275088548364400416034343698204186575808495617;
@begin
    @function(poseidon_hash, @out: 0:1, @in: 0:3)
        $0 <- $0; // Initialize state with input[0]
        $1 <- $1; // Initialize state with input[1]
        $2 <- $2; // Initialize state with input[2]

        // Full rounds (first half)
        // Round 0
        $0 <- @addc($0, $<1>); // Add round constant
        $1 <- @addc($1, $<2>); // Add round constant
        $2 <- @addc($2, $<3>); // Add round constant
        
        $t0 <- @mul($0, $0); // x^2
        $t3 <- @mul($t0, $t0); // x^4
        $0 <- @mul($t3, $0); // x^5
        
        $t1 <- @mul($1, $1); // x^2
        $t4 <- @mul($t1, $t1); // x^4
        $1 <- @mul($t4, $1); // x^5
        
        $t2 <- @mul($2, $2); // x^2
        $t5 <- @mul($t2, $t2); // x^4
        $2 <- @mul($t5, $2); // x^5
        
        $t6 <- @mulc($0, $<2>) @add @mulc($1, $<1>) @add @mulc($2, $<1>); // MDS row 0
        $t7 <- @mulc($0, $<1>) @add @mulc($1, $<2>) @add @mulc($2, $<1>); // MDS row 1
        $t8 <- @mulc($0, $<1>) @add @mulc($1, $<1>) @add @mulc($2, $<2>); // MDS row 2
        
        $0 <- $t6; // Update state
        $1 <- $t7; // Update state
        $2 <- $t8; // Update state

        // Round 1
        $0 <- @addc($0, $<4>); // Add round constant
        $1 <- @addc($1, $<5>); // Add round constant
        $2 <- @addc($2, $<6>); // Add round constant
        
        $t0 <- @mul($0, $0); // x^2
        $t3 <- @mul($t0, $t0); // x^4
        $0 <- @mul($t3, $0); // x^5
        
        $t1 <- @mul($1, $1); // x^2
        $t4 <- @mul($t1, $t1); // x^4
        $1 <- @mul($t4, $1); // x^5
        
        $t2 <- @mul($2, $2); // x^2
        $t5 <- @mul($t2, $t2); // x^4
        $2 <- @mul($t5, $2); // x^5
        
        $t6 <- @mulc($0, $<2>) @add @mulc($1, $<1>) @add @mulc($2, $<1>); // MDS row 0
        $t7 <- @mulc($0, $<1>) @add @mulc($1, $<2>) @add @mulc($2, $<1>); // MDS row 1
        $t8 <- @mulc($0, $<1>) @add @mulc($1, $<1>) @add @mulc($2, $<2>); // MDS row 2
        
        $0 <- $t6; // Update state
        $1 <- $t7; // Update state
        $2 <- $t8; // Update state

        // Round 2
        $0 <- @addc($0, $<7>); // Add round constant
        $1 <- @addc($1, $<8>); // Add round constant
        $2 <- @addc($2, $<9>); // Add round constant
        
        $t0 <- @mul($0, $0); // x^2
        $t3 <- @mul($t0, $t0); // x^4
        $0 <- @mul($t3, $0); // x^5
        
        $t1 <- @mul($1, $1); // x^2
        $t4 <- @mul($t1, $t1); // x^4
        $1 <- @mul($t4, $1); // x^5
        
        $t2 <- @mul($2, $2); // x^2
        $t5 <- @mul($t2, $t2); // x^4
        $2 <- @mul($t5, $2); // x^5
        
        $t6 <- @mulc($0, $<2>) @add @mulc($1, $<1>) @add @mulc($2, $<1>); // MDS row 0
        $t7 <- @mulc($0, $<1>) @add @mulc($1, $<2>) @add @mulc($2, $<1>); // MDS row 1
        $t8 <- @mulc($0, $<1>) @add @mulc($1, $<1>) @add @mulc($2, $<2>); // MDS row 2
        
        $0 <- $t6; // Update state
        $1 <- $t7; // Update state
        $2 <- $t8; // Update state

        // Round 3
        $0 <- @addc($0, $<10>); // Add round constant
        $1 <- @addc($1, $<11>); // Add round constant
        $2 <- @addc($2, $<12>); // Add round constant
        
        $t0 <- @mul($0, $0); // x^2
        $t3 <- @mul($t0, $t0); // x^4
        $0 <- @mul($t3, $0); // x^5
        
        $t1 <- @mul($1, $1); // x^2
        $t4 <- @mul($t1, $t1); // x^4
        $1 <- @mul($t4, $1); // x^5
        
        $t2 <- @mul($2, $2); // x^2
        $t5 <- @mul($t2, $t2); // x^4
        $2 <- @mul($t5, $2); // x^5
        
        $t6 <- @mulc($0, $<2>) @add @mulc($1, $<1>) @add @mulc($2, $<1>); // MDS row 0
        $t7 <- @mulc($0, $<1>) @add @mulc($1, $<2>) @add @mulc($2, $<1>); // MDS row 1
        $t8 <- @mulc($0, $<1>) @add @mulc($1, $<1>) @add @mulc($2, $<2>); // MDS row 2
        
        $0 <- $t6; // Update state
        $1 <- $t7; // Update state
        $2 <- $t8; // Update state

        // Partial rounds (simplified to just 3 for brevity)
        // Partial Round 0
        $0 <- @addc($0, $<13>); // Add round constant
        $1 <- @addc($1, $<14>); // Add round constant
        $2 <- @addc($2, $<15>); // Add round constant
        
        $t0 <- @mul($0, $0); // x^2
        $t1 <- @mul($t0, $t0); // x^4
        $0 <- @mul($t1, $0); // x^5 (only apply S-box to first element)
        
        $t2 <- @mulc($0, $<2>) @add @mulc($1, $<1>) @add @mulc($2, $<1>); // MDS row 0
        $t3 <- @mulc($0, $<1>) @add @mulc($1, $<2>) @add @mulc($2, $<1>); // MDS row 1
        $t4 <- @mulc($0, $<1>) @add @mulc($1, $<1>) @add @mulc($2, $<2>); // MDS row 2
        
        $0 <- $t2; // Update state
        $1 <- $t3; // Update state
        $2 <- $t4; // Update state

        // Partial Round 1
        $0 <- @addc($0, $<16>); // Add round constant
        $1 <- @addc($1, $<17>); // Add round constant
        $2 <- @addc($2, $<18>); // Add round constant
        
        $t0 <- @mul($0, $0); // x^2
        $t1 <- @mul($t0, $t0); // x^4
        $0 <- @mul($t1, $0); // x^5 (only apply S-box to first element)
        
        $t2 <- @mulc($0, $<2>) @add @mulc($1, $<1>) @add @mulc($2, $<1>); // MDS row 0
        $t3 <- @mulc($0, $<1>) @add @mulc($1, $<2>) @add @mulc($2, $<1>); // MDS row 1
        $t4 <- @mulc($0, $<1>) @add @mulc($1, $<1>) @add @mulc($2, $<2>); // MDS row 2
        
        $0 <- $t2; // Update state
        $1 <- $t3; // Update state
        $2 <- $t4; // Update state

        // Partial Round 2
        $0 <- @addc($0, $<19>); // Add round constant
        $1 <- @addc($1, $<20>); // Add round constant
        $2 <- @addc($2, $<21>); // Add round constant
        
        $t0 <- @mul($0, $0); // x^2
        $t1 <- @mul($t0, $t0); // x^4
        $0 <- @mul($t1, $0); // x^5 (only apply S-box to first element)
        
        $t2 <- @mulc($0, $<2>) @add @mulc($1, $<1>) @add @mulc($2, $<1>); // MDS row 0
        $t3 <- @mulc($0, $<1>) @add @mulc($1, $<2>) @add @mulc($2, $<1>); // MDS row 1
        $t4 <- @mulc($0, $<1>) @add @mulc($1, $<1>) @add @mulc($2, $<2>); // MDS row 2
        
        $0 <- $t2; // Update state
        $1 <- $t3; // Update state
        $2 <- $t4; // Update state

        // Full rounds (second half)
        // Round 0
        $0 <- @addc($0, $<22>); // Add round constant
        $1 <- @addc($1, $<23>); // Add round constant
        $2 <- @addc($2, $<24>); // Add round constant
        
        $t0 <- @mul($0, $0); // x^2
        $t3 <- @mul($t0, $t0); // x^4
        $0 <- @mul($t3, $0); // x^5
        
        $t1 <- @mul($1, $1); // x^2
        $t4 <- @mul($t1, $t1); // x^4
        $1 <- @mul($t4, $1); // x^5
        
        $t2 <- @mul($2, $2); // x^2
        $t5 <- @mul($t2, $t2); // x^4
        $2 <- @mul($t5, $2); // x^5
        
        $t6 <- @mulc($0, $<2>) @add @mulc($1, $<1>) @add @mulc($2, $<1>); // MDS row 0
        $t7 <- @mulc($0, $<1>) @add @mulc($1, $<2>) @add @mulc($2, $<1>); // MDS row 1
        $t8 <- @mulc($0, $<1>) @add @mulc($1, $<1>) @add @mulc($2, $<2>); // MDS row 2
        
        $0 <- $t6; // Update state
        $1 <- $t7; // Update state
        $2 <- $t8; // Update state

        // Round 1
        $0 <- @addc($0, $<25>); // Add round constant
        $1 <- @addc($1, $<26>); // Add round constant
        $2 <- @addc($2, $<27>); // Add round constant
        
        $t0 <- @mul($0, $0); // x^2
        $t3 <- @mul($t0, $t0); // x^4
        $0 <- @mul($t3, $0); // x^5
        
        $t1 <- @mul($1, $1); // x^2
        $t4 <- @mul($t1, $t1); // x^4
        $1 <- @mul($t4, $1); // x^5
        
        $t2 <- @mul($2, $2); // x^2
        $t5 <- @mul($t2, $t2); // x^4
        $2 <- @mul($t5, $2); // x^5
        
        $t6 <- @mulc($0, $<2>) @add @mulc($1, $<1>) @add @mulc($2, $<1>); // MDS row 0
        $t7 <- @mulc($0, $<1>) @add @mulc($1, $<2>) @add @mulc($2, $<1>); // MDS row 1
        $t8 <- @mulc($0, $<1>) @add @mulc($1, $<1>) @add @mulc($2, $<2>); // MDS row 2
        
        $0 <- $t6; // Update state
        $1 <- $t7; // Update state
        $2 <- $t8; // Update state

        // Round 2
        $0 <- @addc($0, $<28>); // Add round constant
        $1 <- @addc($1, $<29>); // Add round constant
        $2 <- @addc($2, $<30>); // Add round constant
        
        $t0 <- @mul($0, $0); // x^2
        $t3 <- @mul($t0, $t0); // x^4
        $0 <- @mul($t3, $0); // x^5
        
        $t1 <- @mul($1, $1); // x^2
        $t4 <- @mul($t1, $t1); // x^4
        $1 <- @mul($t4, $1); // x^5
        
        $t2 <- @mul($2, $2); // x^2
        $t5 <- @mul($t2, $t2); // x^4
        $2 <- @mul($t5, $2); // x^5
        
        $t6 <- @mulc($0, $<2>) @add @mulc($1, $<1>) @add @mulc($2, $<1>); // MDS row 0
        $t7 <- @mulc($0, $<1>) @add @mulc($1, $<2>) @add @mulc($2, $<1>); // MDS row 1
        $t8 <- @mulc($0, $<1>) @add @mulc($1, $<1>) @add @mulc($2, $<2>); // MDS row 2
        
        $0 <- $t6; // Update state
        $1 <- $t7; // Update state
        $2 <- $t8; // Update state

        // Round 3
        $0 <- @addc($0, $<31>); // Add round constant
        $1 <- @addc($1, $<32>); // Add round constant
        $2 <- @addc($2, $<33>); // Add round constant
        
        $t0 <- @mul($0, $0); // x^2
        $t3 <- @mul($t0, $t0); // x^4
        $0 <- @mul($t3, $0); // x^5
        
        $t1 <- @mul($1, $1); // x^2
        $t4 <- @mul($t1, $t1); // x^4
        $1 <- @mul($t4, $1); // x^5
        
        $t2 <- @mul($2, $2); // x^2
        $t5 <- @mul($t2, $t2); // x^4
        $2 <- @mul($t5, $2); // x^5
        
        $t6 <- @mulc($0, $<2>) @add @mulc($1, $<1>) @add @mulc($2, $<1>); // MDS row 0
        $t7 <- @mulc($0, $<1>) @add @mulc($1, $<2>) @add @mulc($2, $<1>); // MDS row 1
        $t8 <- @mulc($0, $<1>) @add @mulc($1, $<1>) @add @mulc($2, $<2>); // MDS row 2
        
        $0 <- $t6; // Update state
        $1 <- $t7; // Update state
        $2 <- $t8; // Update state

        // Output the first element as the hash result
        $0 <- $0; // Output hash result
    @end
@end
